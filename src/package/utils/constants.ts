import { Theme } from '../types';

export const THEME_DEFAULT: Theme = {
  text: 'dimgray',
  paper: 'white',
  textBold: 'black',
  textLight: 'lightgray',
};
export const ICON_WIDTH_DEFAULT = 24;
export const SPECIAL_KEY_REGEX = /^Shift|CapsLock|Tab|Alt| $/;

// Deps $textarea-padding
export const TEXTAREA_PADDING = 8;
